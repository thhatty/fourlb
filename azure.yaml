
# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: tdd-lb-collection
metadata:
    template: tdd-lb-collection@0.0.1

infra:
  provider: "bicep"
  resourceGroup: rg-common-${AZURE_ENV_NAME}
  
   

services:
  webapp:
    project: ./WebSite
    host: appservice
    language: dotnet
    resourceGroup: rg-fd-${AZURE_ENV_NAME}
